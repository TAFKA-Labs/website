import { createGlobalStyle } from 'styled-components'

export default createGlobalStyle`
  html {
    background-color: #f4f1f1;
  }

  body, main, section, fieldset, header {
    padding: 0;
    margin: 0;
  }

  h3 {
    margin-top: 2.56rem;
  }
`
