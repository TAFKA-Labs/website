import styled from 'styled-components'

const CardDividerText = styled.div`
  flex: 1;
  font-family: ${({ theme }) => theme.typography.accent}, sans-serif;
  text-align: center;
  margin: 1.5em;
`

export default CardDividerText
